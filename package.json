{
  "name": "dynamic-report-card-poc",
  "version": "1.0.0",
  "private": true,
  "description": "Proof-of-concept dynamic exam/report card module (Express + Angular)",
  "scripts": {
    "server": "npm --prefix server run start",
    "client": "npm --prefix client run start",
    "start": "concurrently -n server,client -c blue,green \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
