// Manual grade overrides, keyed by (studentId, subjectId, schemeId). Empty
// by default — a row only appears once a teacher explicitly overrides the
// auto-computed grade for that student's subject.
const gradeOverrides = [];

module.exports = gradeOverrides;
